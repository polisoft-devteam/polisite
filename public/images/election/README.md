# Val 2026

The photograph behind the heading on /election. Every `.webp` in here is used, in filename
order, the same way `hero/` works.

Drop the original in and run `pnpm images:optimize`. Empty is fine: the page falls back to
a plain heading.
